Three workspaces: `core` (UI-agnostic engine — the contract), `cli` (thin consumer), `raycast`
(thin consumer). See the repo root `CLAUDE.md` for architecture and `docs/BUILD_PLAN.md` for
build status.
