{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "reclaimd",
  "title": "reclaimd",
  "description": "Annotated disk-space findings — what's reclaimable, what it costs to get back. Never deletes; copies commands for you to run.",
  "icon": "icon.png",
  "author": "reclaimd",
  "categories": [
    "System"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "list",
      "title": "Scan For Reclaimable Space",
      "subtitle": "reclaimd",
      "description": "List disk-space findings with annotations",
      "mode": "view",
      "preferences": [
        {
          "name": "includeSlow",
          "type": "checkbox",
          "required": false,
          "title": "Slow detectors",
          "label": "Include slow / opt-in detectors",
          "description": "Run higher-cost detectors (e.g. Time Machine snapshots, orphaned node_modules). None exist until a later build step; the toggle is wired now.",
          "default": false
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^2.0.3",
    "@reclaimd/core": "*"
  },
  "devDependencies": {
    "@types/react": "^19.0.10",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
